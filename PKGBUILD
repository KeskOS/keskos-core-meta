pkgname=keskos-core-meta
pkgver=0.1.0
pkgrel=4
pkgdesc="Meta package for the basic KeskOS userspace and maintenance toolset"
arch=(any)
url="https://github.com/memegeko/keskos"
license=(GPL-3.0-or-later)
depends=(
  btop
  fastfetch
  keskos-keyring
  keskos-mirrorlist
  keskos-release
  keskos-tools
  networkmanager
  bluez
  bluez-utils
  usbutils
)
optdepends=(
  'base-devel: development tools for AUR/package building when requested'
  'git: source control and AUR helper bootstrap when requested'
  'cups: printing daemon for optional printing support'
  'print-manager: Plasma printing UI for optional printing support'
  'gparted: graphical disk partition editor'
  'partitionmanager: KDE partition editor'
  'timeshift: snapshot/restore utility'
  'kdeconnect: phone integration utility'
)
source=()
sha256sums=()

package() {
  :
}
